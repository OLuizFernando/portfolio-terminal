node_modules/
dist/
.venv/
__pycache__/
src/generated/
.DS_Store

# Fontes do DOOM: baixadas por ./wasm/fetch-sources.sh, só necessárias para
# recompilar. Os artefatos prontos ficam versionados em public/doom/.
vendor/
wasm/build/

# Telemetria em desenvolvimento. Em produção mora no StateDirectory do systemd.
# A barra inicial ancora a regra na raiz: sem ela, o padrão casava também com
# content/en/var/, e todo arquivo novo do /var simulado sumia do commit em
# silêncio — entrava no manifesto, aparecia em dev e não chegava ao deploy.
/var/
